<h3>Do you want to tweet with this content?</h3>

<%= form_with(model: @tweet, url: tweets_path, local: true) do |form| %>
    <%= form.hidden_field :content %>

    <p>Body:<%= @tweet.content %></p>
  <%= form.submit  "submit" %>
<% end %>